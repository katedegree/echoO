services:
  web:
    build: ./web
    volumes:
      - ./web:/web
    ports:
      - 3000:3000
  api:
    build: ./api
    volumes:
      - ./api:/api
      - ./api/php.ini:/usr/local/etc/php/php.ini
    ports:
      - 8080:8000
